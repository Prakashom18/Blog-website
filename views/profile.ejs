<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>

    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="bg-red-500 inline-block px-3 py-2 text-sm mb-5 outline-none rounded-md">Logout</a>
        </div>
        <div class="flex  items-start gap-2  ">
            <div class="w-10 h-10  rounded-md overflow-hidden">  <img class="w-full h-full
                object-cover" src="/images/uploads/<%=user.profilepic%>" alt="profilepic"></div>
        <h3 class="text-3xl mb-5 mt-2"> 
           <span class="font-light"> Hello </span>, <%=user.name%>👋</h3>    
        </div>
        
        
        
        <h5 class="mb-5"> Create your post here</h5>
        <form action="/post" method="post">
            
            <textarea name="content"  class="p-3 w-1/3 resize-none outline-none bg-transparent border-2 border-zinc-800 rounded-md block" placeholder="what's on your mind?">
            </textarea>
            <input type="submit" class="outline-none w-40 bg-blue-800 rounded-md px-3 py-2 block mt-2 text-sm" value="Create your post">


        </form>
        <div class="posts mt-20">
          <h3 class="text-zinc-400"> Your Posts: </h3> 
          <div class="postcontainer mt-5">
               <%user.posts.reverse().forEach(function(post){    %>           
                <div class="post w-1/3 p-4 mb-4 border-2 border-zinc-700 rounded-md">
                    <h4 class="text-blue-400"><%=user.username%></h4>   
                <p class="text-sm tracking-tight"><%=post.content%> </p>
                <div class="inline-block mt-2"> <%= post.likes.length %> likes</div>     
                    <div class="btns flex gap-5 mt-4">
                        <a href="/like/<%=post._id%>" class="text-blue-500">
                            <%= post.likes.indexOf(user._id) === -1 ? "Like" :"Unlike" %></a>
                         <a  class=" text-zinc-400" href="/edit/<%=post._id%>"> Edit</a>
               
                    </div>
                   
             </div>
                <%})%>              
          </div>

        </div>
    </div>
    
</body>
</html>