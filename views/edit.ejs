<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
     <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <h3 class="text-3xl mb-5 "> 
        <span class="font-light"> Hello </span></h3>
        <h5 class="mb-5"> Edit your post</h5>
        <form action="/update/<%=post._id%>" method="post">
            
            <textarea name="content"  class="p-3 w-1/3 resize-none outline-none bg-transparent border-2 border-zinc-800 rounded-md block" placeholder="what's on your mind?">
                <%=post.content%>
            </textarea>
            <input type="submit" class="outline-none w-40 bg-yellow-500 rounded-md px-3 py-2 block mt-2 text-sm" value="Update post">


        </form>
     </div>
</body>
</html>